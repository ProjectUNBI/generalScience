import 'package:flutter/material.dart';

class BiologyHome extends StatelessWidget {
  BiologyHome();
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("Biology"),
        centerTitle: true,
      ),
    );
  }
}
import 'package:flutter/material.dart';

class ChemestryHome extends StatelessWidget {
  ChemestryHome();
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("chemestry"),
        centerTitle: true,
      ),
    );
  }
}